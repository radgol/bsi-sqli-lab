json_output: true

stdout_output:
  enabled: true

file_output:
  enabled: true
  keep_alive: true
  filename: /var/log/falco-bsi.log

append_output:
  - match:
      source: syscall
    extra_output: " container=%container.name proc=%proc.name pid=%proc.pid user=%user.name"
